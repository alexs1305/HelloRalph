who_is_ralph.colorize.sh - wrapper

Usage:

  ./scripts/who_is_ralph.colorize.sh

This wrapper executes the existing who_is_ralph.sh script and colorizes every exact occurrence of "Ralph" with ANSI purple (\x1b[35m). The wrapper is non-invasive and does not modify the original script file.
